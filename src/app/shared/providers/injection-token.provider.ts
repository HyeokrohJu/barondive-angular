import { InjectionToken } from '@angular/core';

/**
 * Moment DateTime Full Format
 */
export const MOMENT_FULL_DATE = new InjectionToken<string>('moment.full.date');

/**
 * Moment DateTime Minutes Format
 */
export const MOMENT_MIN_DATE = new InjectionToken<string>('moment.min.date');

/**
 * Moment Date Format
 */
export const MOMENT_DATE = new InjectionToken<string>('moment.date');
